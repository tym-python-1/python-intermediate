# Python script to read a file and print countries with their capitals using readline()

def print_countries_and_capitals(filename):
    # Open the file in read mode
        with  as :
          # Read one line from the file
          line = 
          while :
            # Split each line into country and capital using comma as the delimiter
            
            # Print formatted output using f-strings
            print()
            # Read the next line from the file
            line = 
              
# Specify the path to the file with countries and capitals
filename = 

# Call the function with the filename
print_countries_and_capitals(filename)


