# Initialize sets for departments with the resource IDs
it_resources = 
hr_resources = 

# Initialize a dictionary to hold resource details using resource ID as key
resource_details = {
    
}


# Perform union of both sets to find all unique resources
all_resources = 

# Perform intersection to find resources that are shared between IT and HR
shared_resources = 

# User inputs a resource ID to get details
resource_id = 
if  in :
    # Unpack the tuple containing resource details
     
    print()
else:
    print("Resource ID not found.")
